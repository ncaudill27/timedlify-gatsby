type User {
  
  completed: Boolean!
}

type Timedlify {
  name: String!
  playlist: String!
  color: String!
  hours: Int
  minutes
  seconds
  isInterval
  restInterval
}

type Query {
  allTodos: [Todo!]
  todosByCompletedFlag(completed: Boolean!): [Todo!]
}
